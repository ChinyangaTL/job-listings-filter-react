import React from 'react';

const Header = () => {
  return (
    <header className="bg-cyan-primary ">
      <img src="/images/bg-header-desktop.svg" alt="bh header" />
    </header>
  );
};

export default Header;
